@page "/triage"
@inject NavigationManager Nav

@* The triage UI now lives as the "K zařazení" tab on the Rules page; keep the old URL working. *@
@code {
    protected override void OnInitialized() => Nav.NavigateTo("rules?tab=triage", replace: true);
}
