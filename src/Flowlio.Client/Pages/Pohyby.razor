@page "/pohyby"
@attribute [Authorize]
@inject NavigationManager Nav

@* Manual movement entry and batch management moved into the Transactions page. Keep this route as a
   redirect so existing bookmarks and links land on the consolidated view. *@
@code {
    protected override void OnInitialized() => Nav.NavigateTo("transactions", replace: true);
}
